This project is a spring boot Rest Api that acts like a simple chatbot it accepts a json request containing  a message,process it in the service layer using conditional logic,and returns a response the arichitecutre follows a layered approach with controller handling requests,Service managing bussiness logic,and model representing data,currently it process messages without stroing them in the in the databases
